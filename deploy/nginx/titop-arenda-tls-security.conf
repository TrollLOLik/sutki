# Include this file from each HTTPS server block only.
#
# Do not add includeSubDomains or preload: titop.ru is shared by several
# products, and those flags would require every current and future subdomain
# to support HTTPS without exception.
add_header Strict-Transport-Security "max-age=31536000" always;
server_tokens off;
